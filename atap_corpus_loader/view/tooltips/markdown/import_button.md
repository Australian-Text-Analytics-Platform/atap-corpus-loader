Select one or more .atap files and click the **Import corpus (ATAP)** button.

Non-atap files cannot be imported - instead load and build these files.

Each imported .atap file will create one corpus.